# API 키 절대 커밋 금지
.env

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/

# 로그 (선택적으로 제외)
signals.log

# 모델 바이너리 (용량 클 경우)
# ml/saved_models/*.pkl

# OS
.DS_Store
Thumbs.db
*.pyc
__pycache__/

# 1m/3m 합산 파일은 GitHub 100MB 제한 초과 — 연도별 파일만 커밋
data/*_1m_all.csv.gz
data/*_3m_all.csv.gz
# 봇 런타임 상태. 저장소에 두면 VPS에서 git pull 한 번에 살아있는
# 포지션 기록이 덮어써진다. 파일이 없으면 봇이 기본값으로 시작한다.
bot/oversold/state.json
bot/cycle_state.json
bot/ma_bot_state.json
bot/upbit_watch_state.json
state/
